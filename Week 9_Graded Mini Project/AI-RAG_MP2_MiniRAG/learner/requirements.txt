openai>=1.40.0
qdrant-client>=1.10.0
python-dotenv>=1.0.0      # optional — for loading .env if you don't 'source' it

# ─── Stretch (W9 hybrid+rerank) — uncomment if you want to try the Stretch path
# rank-bm25>=0.2.2
# sentence-transformers>=3.0.0
