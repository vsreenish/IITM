# W4 — extends W3 requirements with tiktoken for pre-call token counting.
# Pin major versions; minors may vary by environment.

# From W2 / W3 (unchanged)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
httpx>=0.26,<1.0
openai>=1.40,<2.0
streamlit>=1.30,<2.0
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0

# New in W4
tiktoken>=0.7,<1.0
